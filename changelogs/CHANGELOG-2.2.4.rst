Changelog for version 2.2.4
============================

Gitcd commands
##############

- git-cd can now run in subdirectories
- git-cd start now ask permissions to stash changes and pop them after branch creation

Generic changes
###############

- Tests run as github actions instead of travis jobs
- Publication in pypi.io handled over github actions